# Docker Role

This Ansible role installs and configures Docker on Ubuntu.

## Tasks
- Removes old Docker packages
- Adds Docker official repository and GPG key
- Installs Docker packages
- Ensures Docker service is running and enabled

